# 公开前准备清单（58系统 / Wubot）

> 用途：仓库从私有转公开前，逐项核对。本清单记录「已完成」与「待执行」，所有改动遵循铁律——**commit / push 须经用户明确许可**。
> 最后更新：2026-08-18

## 当前状态快照

- 本地历史已用 `git filter-repo --replace-text` **重写**，全量 grep 验证：**任何 commit 均不含真实 QQ / 群号 / 姓名 / token**。
- 工作区干净，5 个 commit，HEAD = `cde2f77`。
- `origin` 已重新指回 `https://github.com/DragonUgin/5botSystem.git`。
- 备份：`D:/58_before_filter.bundle`（172 KB，可回滚重写前的历史）。
- **尚未 push**：远程 `main` 仍是重写前的旧历史，强推前请勿让他人 clone/fork 旧链接。

---

## ✅ 已完成（本地）

- [x] 删除 6 个含敏感信息的文件：`gen_test_xlsx.py` / `TEST_PLAN_7PLAYERS.md` / `demo.html` / `dryrun_import.py` / `verify_crud.py` / `verify_delist.py`
- [x] 合并删除 `README.txt`（独有内容并入 `README.md`）、`Claw/待修复备忘录.md`（与根目录版合并为一份）
- [x] 脱敏 5 个文件：`gen_sample_data.py`、`gen_clue_dew_xlsx.py`（14 猎人姓名→占位）、`web/index.html`、`指令清单.md`、`PLAN_FRONTEND.md`（→`docs/前端方案.md`）
- [x] `实战赐福颂.md` 为未跟踪文件，未纳入任何提交，不会上传
- [x] 提交 `010d638` 经历史重写现为 `cde2f77`（`chore: 清理敏感信息并合并文档`）
- [x] LICENSE（GPL-3.0-or-later）、34 个 `.py` 的 SPDX 头、CHANGELOG、THIRD_PARTY_NOTICES、`.gitignore` 数据分离、`.env.example` 空值模板 — 均已就绪
- [x] `docs/` 全套指南（玩家/猎人/任务点NPC/管理员/通用/游戏介绍/前端方案）已落盘

---

## 🔴 P0 — 阻塞项（公开前必须做）

- [ ] **强推重写后的历史到 GitHub**（不可逆，需用户点头）
  - 命令：`git push --force-with-lease origin main`
  - 风险：改写远程全部 commit hash；若 GitHub 对 `main` 开启**分支保护**，会拒绝强推 → 需临时关闭保护或换分支。
  - 前置：强推前确认无他人已 fork / clone 旧链接（旧历史将失效）。

- [ ] **轮换凭据（手动，用户本机操作）**
  - 改 QQ bot 账号与 bot 小号（`3760643269` 等）的登录密码；
  - NapCat 重新扫码登录，刷新 token，使旧 `Zzx_132435` 失效；
  - 若有长期固定 token，重新生成一份。
  - 理由：重写历史不保证旧副本（fork/clone/缓存/CI 日志）彻底消失，轮换让旧值作废双保险。

- [x] **修 D1：Web API 无认证** ✅ 已完成
  - 后端：`config.py` 加 `get_admin_token()`；`api.py` 加 `/api/*` Bearer Token 中间件（fail-closed，空 Token 时全部返回 401）；`bot.py` 启动时告警。
  - 前端：`index.html` 加登录框（输入 ADMIN_TOKEN，存 localStorage）、`api()` 自动带 `Authorization` 头、导入/导出接口带 Token、401 自动弹登录框并停轮询。
  - `.env.example` 已加 `ADMIN_TOKEN` 字段说明。

---

## 🟡 P1 — 建议项（公开体验 / 规范）

- [x] **补 `SECURITY.md`** ✅：漏洞私有上报渠道 + `:8080` 认证现状与部署须知（强 Token、轮换凭据）
- [x] **补 `CODE_OF_CONDUCT.md`** ✅：Contributor Covenant 2.1（中文）
- [x] **补 `CONTRIBUTING.md`** ✅：提交规范（subject + body 逐文件说明）、`docs/` 指令格式、GPL-3.0 版权要求、反馈渠道
- [ ] **GitHub 仓库设置**：About 描述 + Topics（草稿见下，需用户在 GitHub 后台填写）
  - Description（英文草稿）：
    `A NapCat-based QQ real-time team-battle game system (Wubot 极限挑战). Three-group broadcast coordination, web dashboard, and dew/clue/power-card item tracking. Built on NoneBot2 + FastAPI + Vue3 + SQLite.`
  - Topics：`nonebot2` `napcat` `qq-bot` `qq-group-bot` `fastapi` `vue3` `game-system` `realtime-game`

---

## 🟢 可选增强（非阻塞）

- [x] **新增「其他 NPC」指南**（`docs/其他NPC指南.md`）：露水 NPC / 机动人员 / 跟组辅导员（露水 NPC 有与猎人相同的功能卡指令但不淘汰玩家；机动 / 跟组为线下角色无 bot 指令）。README 文档索引已加一行。
- [ ] 合并根目录 `待修复备忘录.md` 与 `Claw/` 旧版 — 已完成，无需再动
- [ ] `README.txt` 与 `README.md` 重复 — 已删除 txt，无需再动

---

## 🔙 备份与回滚

- 回滚重写前的历史：`git clone D:/58_before_filter.bundle old_history`（bundle 包含所有旧 commit 与分支）
- 回滚强推：先用 bundle 恢复本地，再 `git push --force`；注意这会再次改写历史，需谨慎。

---

## 执行顺序建议

1. 用户手动**轮换凭据**（P0，与代码无关，可立即做）
2. 落 D1 Web API 认证修复（P0，需改代码 + commit）
3. 补 P1 三份文档 + GitHub 设置
4. 全部就绪后，**用户确认 → 强推**（P0，不可逆）
5. 强推后核对 GitHub 页面无残留敏感、分支保护按需恢复
