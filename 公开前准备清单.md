# 公开前准备清单（58系统 / Wobot-System）

> 用途：仓库从私有转公开前，逐项核对。本清单记录「已完成」与「待执行」，所有改动遵循铁律——**commit / push 须经用户明确许可**。
> 最后更新：2026-08-18

## 当前状态快照

- 本地历史已用 `git filter-repo --replace-text` **重写**，全量 grep 复核（含 `dfafc92`）：**任何 commit 均不含真实 QQ / 群号 / 姓名 / token 等功能性敏感数据**。
  - 注：扫描仅 3 处命中，均在文档自引用中——`CONTRIBUTING.md` 的提交身份邮箱 `562993317@qq.com`（公开作者身份，有意保留）、本清单「轮换凭据」条目的提醒文字（`3760643269` / `Zzx_132435`），均非泄露。
- 工作区干净，7 个 commit，HEAD = `dfafc92`。
- **推送策略已定：删库重开（不 force push）**——无 fork / 无 clone 副本流出，删旧仓库比强推更彻底、零悬空对象。
- **凭据轮换已全部完成**（P0 阻塞项清零）：GitHub OAuth 授权已撤销 / QQ bot 密码已改 / NapCat WebUI token `Zzx_132435` 已换新为 `Nc_…`（gitignored 本机文件，不进仓库）。
- 备份：`D:/58_before_filter.bundle`（192 KB，旧历史保险，**仅留本地、不推送**，已确认不会随新仓库上传）。
- **尚未 push 到新仓库**：等用户在 GitHub 删除旧私有仓库并重建后，更新 remote 再普通 push。

---

## ✅ 已完成（本地）

- [x] 删除 6 个含敏感信息的文件：`gen_test_xlsx.py` / `TEST_PLAN_7PLAYERS.md` / `demo.html` / `dryrun_import.py` / `verify_crud.py` / `verify_delist.py`
- [x] 合并删除 `README.txt`（独有内容并入 `README.md`）、`Claw/待修复备忘录.md`（与根目录版合并为一份）
- [x] 脱敏 5 个文件：`gen_sample_data.py`、`gen_clue_dew_xlsx.py`（14 猎人姓名→占位）、`web/index.html`、`指令清单.md`、`PLAN_FRONTEND.md`（→`docs/前端方案.md`）
- [x] `实战赐福颂.md` 为未跟踪文件，未纳入任何提交，不会上传
- [x] 提交 `010d638` 经历史重写现为 `cde2f77`（`chore: 清理敏感信息并合并文档`）
- [x] LICENSE（GPL-3.0-or-later）、34 个 `.py` 的 SPDX 头、CHANGELOG、THIRD_PARTY_NOTICES、`.gitignore` 数据分离、`.env.example` 空值模板 — 均已就绪
- [x] `docs/` 全套指南（玩家/猎人/任务点NPC/管理员/通用/游戏介绍/前端方案）已落盘

---

## 🔴 P0 — 阻塞项（公开前必须做）

- [ ] **删库重开（替代强推，已决策）**
  - 用户在 GitHub 网页**删除**旧私有仓库 `DragonUgin/5botSystem`，并**重建**为 `DragonUgin/Wobot-System`（无 fork、无 clone 流出，删除即彻底清除旧历史与旧 SHA 直达，零悬空对象）。
  - 重建后我执行 `git remote set-url origin git@github.com:DragonUgin/Wobot-System.git`（或对应 HTTPS 地址） + `git push -u origin main`（普通 push，无需 force）。
  - 前置：删除前确认无他人 fork / clone 旧链接（已确认无）。

- [x] **轮换凭据（手动，用户本机操作）** ✅ 已全部完成
  - [x] revoke 此前推送时明文塞进命令 URL 的旧 token（实际为 **OAuth 令牌 `gho_…`，非 classic PAT**）→ 已在 GitHub **Settings → Applications → Authorized OAuth Apps** 撤销 "GitHub CLI" 授权（用户已做）。
  - [x] 改 QQ bot 账号与 bot 小号（`3760643269` 等）的登录密码（用户已做）。
  - [x] `Zzx_132435` 失效：经核查它**不是 QQ 密码，而是 NapCat WebUI 的登录 token**，仅存于本机 `napcat/napcat/config/webui.json`（该目录已被 `.gitignore` 忽略，从未进仓库）。已由助手将其改为新随机值 `Nc_…`（重启 NapCat 后旧值即失效；改这个文件不会进任何提交）。
  - 理由：删库重开只解决「GitHub 仓库侧」，旧凭据若曾泄露则仍可用；轮换让旧值作废，是公开前最后一道保险。

  > **澄清备忘**：`Zzx_132435` = NapCat WebUI token（gitignored，纯本机）；`gho_…` = GitHub OAuth 令牌（归 Applications → Authorized OAuth Apps 管，不在 Tokens (classic) 列表）。两者均非 QQ 登录密码。

- [x] **修 D1：Web API 无认证** ✅ 已完成且**已实测验证成功**
  - 后端：`config.py` 加 `get_admin_token()`；`api.py` 加 `/api/*` Bearer Token 中间件（fail-closed，空 Token 时全部返回 401）；`bot.py` 启动时告警。
  - 前端：`index.html` 加登录框（输入 ADMIN_TOKEN，存 localStorage）、`api()` 自动带 `Authorization` 头、导入/导出接口带 Token、401 自动弹登录框并停轮询。
  - `.env.example` 已加 `ADMIN_TOKEN` 字段说明。
  - D1 验证：用户已确认本地验证成功（`/api/*` 有 token 返 200、无/错 token 返 401）。

---

## 🟡 P1 — 建议项（公开体验 / 规范）

- [x] **补 `SECURITY.md`** ✅：漏洞私有上报渠道 + `:8080` 认证现状与部署须知（强 Token、轮换凭据）
- [x] **补 `CODE_OF_CONDUCT.md`** ✅：Contributor Covenant 2.1（中文）
- [x] **补 `CONTRIBUTING.md`** ✅：提交规范（subject + body 逐文件说明）、`docs/` 指令格式、GPL-3.0 版权要求、反馈渠道
- [ ] **GitHub 仓库设置**：About 描述 + Topics（草稿见下，需用户在 GitHub 后台填写）
  - Description（英文草稿）：
    `A NapCat-based QQ real-time team-battle game system (Wobot Information Management System). Three-group broadcast coordination, web dashboard, and dew/clue/power-card item tracking. Built on NoneBot2 + FastAPI + Vue3 + SQLite.`
  - Description（中文草稿）：
    `基于 NapCat 的 QQ 群实时团队对战游戏系统（Wobot 信息管理系统）。支持三群广播协同、Web 实时数据看板，以及露水 / 线索 / 功能卡等道具的追踪管理。技术栈：NoneBot2 + FastAPI + Vue3 + SQLite。`
  - Topics：`nonebot2` `napcat` `qq-bot` `qq-group-bot` `fastapi` `vue3` `game-system` `realtime-game`

---

## 🟡 容易忽视的清单

以下文档容易被公开准备工作遗漏，需在仓库公开前确认：

- [ ] **`待修复备忘录.md`**：记录已知 bug、设计债、D1/D2 等待修复项。公开前应确认其中无敏感信息，且状态为最新。
- [ ] **`CHANGELOG.md`**：版本更新日志。公开前需补充 1.0.0 之后的改动摘要（建议新增 `[Unreleased]` 段落）。

---

## 🟢 可选增强（非阻塞）

- [x] **新增「其他 NPC」指南**（`docs/其他NPC指南.md`）：露水 NPC / 机动人员 / 跟组辅导员（露水 NPC 有与猎人相同的功能卡指令但不淘汰玩家；机动 / 跟组为线下角色无 bot 指令）。README 文档索引已加一行。
- [ ] 合并根目录 `待修复备忘录.md` 与 `Claw/` 旧版 — 已完成，无需再动
- [ ] `README.txt` 与 `README.md` 重复 — 已删除 txt，无需再动

---

## 🔙 备份与回滚

- 回滚重写前的历史：`git clone D:/58_before_filter.bundle old_history`（bundle 包含所有旧 commit 与分支）
- 回滚强推：先用 bundle 恢复本地，再 `git push --force`；注意这会再次改写历史，需谨慎。

---

## 执行顺序建议

1. 用户手动**轮换凭据**（P0 唯一剩余阻塞项，与代码无关，可立即做；含 revoke 旧 PAT）
2. 用户在 GitHub **删除旧私有仓库** → **重建**新仓库（无 fork/clone，彻底无悬空对象）
3. 我执行 `git remote set-url origin <新地址>` + `git push -u origin main`（普通 push，零 force）
4. 设置 GitHub About 描述 + Topics（P1，用户后台填写，草稿见下）
5. 切 visibility 为 public 前最后核对：页面无残留敏感、分支保护按需开启
