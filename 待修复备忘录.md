# 待修复备忘录

> 通读全系统（2026-08）时发现的 bug / 设计问题 / 死代码 / 崩溃线索。
> 当前仓库已切 GPL-3.0-or-later，数据/系统已分离。下列问题**有空再修**，非紧急。

## 🔴 真 bug（会影响数据正确性）

- **B1 `src/plugins/backend.py` 大盘统计错**
  - "线索未发现"统计用了 `已收集未发现` 状态值，应统计 `已发现未收集` → 恒为 0，且合计不对。
- **B2 `utils/db.py` 撤销露水收集（collect_dew 撤销）状态机断裂**
  - 露水回退到"未收集"，但联动线索仍停在"已收集已发现"；实际应回退到"已发现未收集"。
- **B3 `utils/db.py` 撤销任务点完成（tp_complete 撤销）露水方向反**
  - 撤销时把露水误推成"已发现未收集"，方向反了（应与完成时的标记相反）。

## 🟡 设计问题（逻辑隐患 / 不完整）

- **B4 `utils/db.py` `reveal_clue` 误伤双关联猎人露水**
  - 无条件联动同编号露水 → 误伤双关联猎人的露水（编号 31/33/35/37）。应排除"双关联"标记的露水。
- **B5 `utils/kdocs_sync.py` 整块死功能**
  - 无任何调用，`KDOCS_ENABLED = False`。可整体删除或保留待启用（建议注释说明）。
- **B6 网页 `add_dew_web` 无法新增双关联露水**
  - 用 `INSERT OR IGNORE`，双关联（同 hunter_name 多行）只能靠 Excel 导入，网页编辑无法新增。
- **B7 `revive` 未清猎人抓捕冷却**
  - 复活玩家时未清除其"被猎人抓捕"的冷却计时，可能导致复活后仍被冷却挡住。

## 🟢 死代码清理（可删，无调用）

- `collect_dew` / `collect_clue` / `is_npc` / `get_npc` / `remove_npc` / `replace_task_point_npcs` 系列（去名单化后遗留）
- `src/plugins/work_group.py` 残留 `import incr_task_point_card_collected`（未使用）

## 💥 崩溃线索（实战最后 10 分钟 bot 死机可能相关）

- `src/plugins/timer.py` 的 `while True` 空转循环（无 sleep，CPU 占满风险）
- `src/plugins/debug.py` 全量日志落盘（高负载下 IO 拖慢）
- 前端 `index.html` 多个 `setInterval` 轮询（长时运行可能堆积）

## ⚠️ 安全（待重新决策）

- **D1 Web API 无认证（FastAPI :8080）**
  - 原计划"除了安全漏洞全修"时**故意保留**。
  - 现打算公开仓库；虽 API 绑定 `localhost` 不暴露公网，但一旦部署到服务器即存在风险。
  - **决策（2026-08-17）**：公开前再加，至少加 Token 校验。当前不改代码。
